package com.onb.orderingsystem.domain;

import java.util.HashSet;
import java.util.Set;

import org.junit.Test;

public class CustomerTest {
/*	@Test
	public void TestForTotalOrderPrice(){
		Product product = new Product(1, 5, "Apple", 10);
		Set<OrderItem> totalItems = new HashSet<OrderItem>();
		OrderItem oItem = new OrderItem(1, product.getProdName(), product.getProdQty(),product.getProdUnitPrice());
		totalItems.add(oItem);
		Order order = new Order(1, "Cyril", "Pangilinan", totalItems, order.computeTotalPrice(oItem));
		Set<Order> finalOrder = new HashSet<Order>();
		finalOrder.add(order);
		Customer cust = new Customer(1, 10000, order.getOrderFCustName(), order.getOrderLCustName(), finalOrder);
	}
*/
}
